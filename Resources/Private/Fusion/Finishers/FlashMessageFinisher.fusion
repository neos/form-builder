prototype(Neos.Form.Builder:FlashMessageFinisher.Definition) < prototype(Neos.Form.Builder:Finisher.Definition) {
    formElementType = 'Neos.Form:FlashMessage'

    options {
        messageBody = ${null}
        messageTitle = ''
        messageArguments = Neos.Fusion:RawArray
        messageCode = ${null}
        severity = 'OK'
    }
}