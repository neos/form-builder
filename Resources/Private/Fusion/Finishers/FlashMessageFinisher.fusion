prototype(Neos.Form.Builder:FlashMessageFinisher) < prototype(Neos.Form.Builder:Finisher) {
    formElementType = 'Neos.Form:FlashMessage'

    options {
        messageBody = ${null}
        messageTitle = ''
        messageArguments = Neos.Fusion:RawArray
        messageCode = ${null}
        severity = 'OK'
    }
}