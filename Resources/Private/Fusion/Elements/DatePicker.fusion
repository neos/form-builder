prototype(Neos.Form.Builder:DatePicker) < prototype(Neos.Form.Builder:FormElement) {
    formElementType = 'Neos.Form:DatePicker'

    properties {
        dateFormat = 'Y-m-d'
        initialDate = ${null}
        enableDatePicker = ${false}
    }
}