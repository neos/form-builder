prototype(Neos.Form.Builder:DatePicker.Definition) < prototype(Neos.Form.Builder:FormElement.Definition) {
    formElementType = 'Neos.Form:DatePicker'

    properties {
        dateFormat = 'Y-m-d'
        initialDate = ${null}
        enableDatePicker = ${false}
    }
}