prototype(Neos.Form.Builder:StaticText.Definition) < prototype(Neos.Form.Builder:FormElement.Definition) {
    formElementType = 'Neos.Form:StaticText'

    properties {
        text = ${null}
    }
}